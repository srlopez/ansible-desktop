- name: Install packages 
  vars: 
    packages:
      - gparted 
      - udftools
      - remmina 
      - openssh-server
      - gnome-terminal
      - git
      - curl 
      - wget
      - apt-transport-https
      - ca-certificates
      - gnupg-agent
      - software-properties-common
  
    
  apt: 
    name: "{{ packages }}"